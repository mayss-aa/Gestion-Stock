
{% extends 'base.html.twig' %}
{% block body %}


       
         
        <!-- Checkout Page Start -->
          <form method="post" action="{{ path('app_produit_back_delete', {'id': produit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit.id) }}">
    <button class="btn">Delete</button>
</form>




        <!-- Checkout Page End -->
 
    {% endblock %}
